spring:
  application:
    name: chatzar

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chatzar?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: chatzar
    password: 83492761

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: "CHANGE_ME_TO_LONG_RANDOM_SECRET_CHANGE_ME_TO_LONG_RANDOM_SECRET"
  issuer: "chatzar"
  access-token-exp-minutes: 15
  refresh-token-exp-days: 14